spring:
  cloud:
    aws:
      region:
        static: us-east-1
      credentials:
        access-key: test
        secret-key: test
      endpoint: http://localstack:4566
      sqs:
        enabled: true
      sns:
        enabled: true
